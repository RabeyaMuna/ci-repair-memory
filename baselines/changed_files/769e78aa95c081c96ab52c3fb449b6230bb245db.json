{
    "sha_fail": "769e78aa95c081c96ab52c3fb449b6230bb245db",
    "changed_files": [
        {
            "commit": "769e78aa95c081c96ab52c3fb449b6230bb245db",
            "file_path": "src/axolotl/prompt_strategies/chat_template.py",
            "diff": "diff --git a/src/axolotl/prompt_strategies/chat_template.py b/src/axolotl/prompt_strategies/chat_template.py\nindex a9d26a650..ce68ca482 100644\n--- a/src/axolotl/prompt_strategies/chat_template.py\n+++ b/src/axolotl/prompt_strategies/chat_template.py\n@@ -378,6 +378,12 @@ class ChatTemplateStrategy(PromptTokenizingStrategy):\n         \"\"\"\n         Public method that can handle either a single prompt or a batch of prompts.\n         \"\"\"\n+        def _remove_none_values(obj):\n+            if hasattr(obj, \"items\"):\n+                return {k: _remove_none_values(v) for k, v in obj.items() if v is not None}\n+            elif isinstance(obj, list):\n+                return [_remove_none_values(elem) for elem in obj]\n+            return obj\n \n         if not self.is_prompt_batched(prompt) or not self.supports_batched:\n             return self._tokenize_single_prompt(prompt)\n@@ -385,6 +391,8 @@ class ChatTemplateStrategy(PromptTokenizingStrategy):\n         res = defaultdict(lambda: [])\n         feature_names = list(prompt.keys())\n \n+        prompt = _remove_none_values(prompt)\n+\n         # Process each prompt individually\n         for row in zip(*prompt.values()):\n             tokenized_prompt = self._tokenize_single_prompt(\n"
        }
    ]
}